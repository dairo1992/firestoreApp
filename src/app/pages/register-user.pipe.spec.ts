import { RegisterUSerPipe } from './register-user.pipe';

describe('RegisterUSerPipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterUSerPipe();
    expect(pipe).toBeTruthy();
  });
});
