import { RecoveryPassPipe } from './recovery-pass.pipe';

describe('RecoveryPassPipe', () => {
  it('create an instance', () => {
    const pipe = new RecoveryPassPipe();
    expect(pipe).toBeTruthy();
  });
});
